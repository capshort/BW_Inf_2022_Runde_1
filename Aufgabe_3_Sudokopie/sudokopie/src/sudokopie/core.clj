(ns sudokopie.core
  (:require sudokopie.transformations :as transformations)
  (:gen-class))

(defn- transformation-into
  [s1 s2])

(defn- check-sudokopie
  [s1 s2]
  (some some? (transformation-into s1 s2)))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!" args))




